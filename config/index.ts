/**
 * Application Configuration
 */

export default {
  // Brand configuration
  brand_id: 1,
  brand_name: 'NovelHub',
  
  // API configuration
  apiConfig: {
    isHttpNeedFp: false, // Disable fingerprint for now
    isHttpNeedToken: true, // Enable JWT token authentication
  },
}
