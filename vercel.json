{
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.2 --activate && pnpm install",
  "buildCommand": "pnpm run build",
  "env": {
    "NODE_OPTIONS": "--max_old_space_size=2048",
    "NPM_CONFIG_REGISTRY": "https://registry.npmjs.org/"
  }
}
